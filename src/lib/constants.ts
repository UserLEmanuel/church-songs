// Constante generale de randare.
// Dimensiunile si limitele de font sunt per format, in `formats.ts`.
// Fonturile disponibile sunt in `fonts.ts`.

/** Opacitatea implicita a overlay-ului negru peste fundal. */
export const DEFAULT_OVERLAY = 0.35;

/** Id-ul fundalului folosit implicit pentru pagini noi. */
export const DEFAULT_BACKGROUND_ID = 'bg-01';

/**
 * Sub aceasta marime de font textul devine greu de citit pe proiector,
 * asa ca aratam un avertisment si propunem impartirea pe doua pagini.
 */
export const SMALL_FONT_WARNING = 18;
